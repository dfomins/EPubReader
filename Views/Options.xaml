<Window x:Class="EPubReader.Views.Options"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EPubReader.Views"
        mc:Ignorable="d"
        Title="Options" Height="400" Width="700"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.9*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition />
            </Grid.RowDefinitions>
            <Border Grid.Column="0" BorderBrush="Gray" BorderThickness="1" Margin="0 0 5 10" Padding="10">
                <StackPanel>
                    <Label Content="Font family"/>
                    <ComboBox Name="fontFamilySelect" Cursor="Hand" SelectionChanged="fontFamilyComboBox_SelectionChanged"/>
                    <Label Content="Preview:" Margin="0 5 0 0" />
                    <Label x:Name="fontPrieviewLabel" FontSize="18"/>
                </StackPanel>
            </Border>
            <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Margin="0 0 5 10" Padding="10">
                <StackPanel>
                    <Label Content="Color theme"/>
                    <ComboBox Name="colorThemeSelect" SelectionChanged="colorThemeComboBox_SelectionChanged" Cursor="Hand">
                        <ComboBoxItem Content="White" />
                        <ComboBoxItem Content="Dark" />
                    </ComboBox>
                </StackPanel>
            </Border>
        </Grid>
        <Border x:Name="fontFamilySettingsBlock" Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="5 0 0 10" Padding="10">
            <StackPanel>
                <Label Content="Font size"/>
                <ListBox x:Name="fontSizeSelect">
                    <ListBoxItem Content="10"/>
                    <ListBoxItem Content="12"/>
                    <ListBoxItem Content="14"/>
                    <ListBoxItem Content="16"/>
                    <ListBoxItem Content="18"/>
                    <ListBoxItem Content="20"/>
                    <ListBoxItem Content="24" />
                    <ListBoxItem Content="28" />
                    <ListBoxItem Content="32" />
                    <ListBoxItem Content="36" />
                </ListBox>
                <Label x:Name="currentFontSizeLabel"/>
            </StackPanel>
        </Border>
        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Style="{StaticResource RoundButton}" Width="70" Margin="0 0 20 0" Click="SaveButton_Click"/>
            <Button Content="Close" Style="{StaticResource RoundButton}" Width="70" Margin="0" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
